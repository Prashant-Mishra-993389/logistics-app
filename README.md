# Logistic-App

## Project Structure

- `storefront` -> Marketing and landing frontend
- `dashboard` -> Company operations dashboard frontend
- `admin-dashboard/dashboard` -> Admin operations dashboard frontend
- `driver-dashboard` -> Driver operations dashboard frontend
- `backend` -> Shared Express API for all frontends

## Backend Setup

Use the complete step-by-step backend guide:

- `BACKEND_STEP_BY_STEP.md`

Backend source and API docs:

- `backend/README.md`
